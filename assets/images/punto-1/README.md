# Placeholders de imágenes — Punto 1

Esta carpeta agrupa la evidencia visual del **Punto 1 — Exploración del entorno y sensores (Virtual + QCar real)**.

## Archivos esperados

- `qlabs-quanser-city.png` → captura del entorno virtual (vista general).
- `matlab-imu-plot.png` → plot de IMU en MATLAB.
- `matlab-lidar-plot.png` → visualización LiDAR en MATLAB.
- `qcar-setup.jpg` → foto del setup físico (QCar/QCar2 + entorno de prueba).
- `python-rgb.png` → preview RGB en Python.
- `python-depth.png` → preview depth en Python.
- `python-lidar-map.png` → mapa/scan LiDAR en Python.

## Convención recomendada

- Mantener nombres en minúsculas y separados por guiones.
- Preferir `png` para capturas/plots y `jpg` para fotos de cámara.
- Si hay múltiples corridas, usar sufijo incremental:
  - `python-rgb-run01.png`
  - `python-rgb-run02.png`

## Nota

Si aún no existe una imagen, conservar el nombre reservado en esta lista y dejar constancia en la bitácora del motivo (`TODO(captura pendiente)`).
